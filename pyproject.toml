[project]
name = "discordbot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "AlvaroEng98", email = "alvaro.eng.98@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "discord-py>=2.7.1",
    "python-dotenv>=1.2.3",
]

[project.scripts]
discordbot = "discordbot:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
